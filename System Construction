
Download & install VirtualBox
Download Ubuntu image
Import the Ubuntu image to the VirtualBox and configure the network
  NAT [Network Address Translation] (default mode): use the host ip
                      virtual machine -> host
                      virtual machine -> external internet (if host can access the external internet)
                      DHCP -> virtual machine IP
  NAT Network: plus function based on NAT
               virtual machine <-> virtual machine
               virtual machine <- host (if port forwarding is configured)
  Bridge : regarded as a host in the lan, IP  can belong to the same network segment as the IP address of the physical network adapter of the host machine.  
          virtual machine <-> host 
          virtual machine <-> other server in the Lan of the host
          virtual machine -> external internet
